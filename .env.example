# Copy to `.env` and add your key. Restart `npm.cmd run dev` after changes.
# Create a key: https://aistudio.google.com/app/apikey
#
# If you see "quota" / "limit: 0" for one model, try another (pick one line):
# VITE_GEMINI_MODEL=gemini-2.5-flash
# VITE_GEMINI_MODEL=gemini-2.0-flash-lite
# VITE_GEMINI_MODEL=gemini-1.5-flash

VITE_GEMINI_API_KEY=
